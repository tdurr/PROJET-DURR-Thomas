import { ProductsByCategoryPipe } from './products-by-category.pipe';

describe('ProductsByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
